import React, { FC } from 'react';
import FormsTable from '../../components/content-table/Leads';

interface LeadsProps { }

const Leads: FC<LeadsProps> = () => {
    return <FormsTable />;
};

export default Leads;